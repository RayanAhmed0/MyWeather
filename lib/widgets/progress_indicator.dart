import 'package:flutter/material.dart';

class Circular_progress_indicator extends StatelessWidget {
  const Circular_progress_indicator({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CircularProgressIndicator(
      color: Colors.white,
    );
  }
}
